const areaMap={"Area 1":["Todd Beamer","FWPA","Illahee","Sequoyah","Enterprise","Lakeland","Panther Lake","Rainier View","Sherwood Forest"],"Area 2":["Decatur","TAF @ Saghalie","Truman","Lakota","Olympic View","Adelaide","Brigadoon","Green Gables","Silver Lake","Twin Lakes","ETP/Norman Center"],"Area 3":["FWHS","Sacajawea","Nautilus","Woodmont","Lake Grove","Mark Twain","Mirror Lake","Wildwood","UTS - ECEAP"],"Area 4":["TJHS","Evergreen","Kilo","Camelot","Lake Dolloff","Meredith Hill","Star Lake","Sunnycrest","Valhalla"],"Area 5":["ESC","Security","Nutrition Services","SSC - Maintenance","SSC - Transportation"]};
const areaLeads={"Area 1":"Jerrod / Nicholle","Area 2":"Michael / Bobby","Area 3":"John / Megan","Area 4":"Pat / Sushant","Area 5":"Tier 3"};
let schools=Object.entries(areaMap).flatMap(([area,names],areaIndex)=>names.map((name,index)=>{const total=12+((index*5+areaIndex*7)%24);const done=Math.min(total,Math.floor(total*([.91,.43,.74,.25,.59,.82,.33,.55,.96,.22,.68][index%11])));const damaged=(index*3+areaIndex)%9;const interns=1+((index+areaIndex)%5);return{name,area,total,done,damaged,interns}}));
let notes=[{school:"Evergreen",area:"Area 4",text:"Asset 567034 is damaged. Screen issue noted and device needs swap.",user:"Jayden",time:"9:42 AM"},{school:"Olympic View",area:"Area 2",text:"Two carts fully cleaned and powerwashed. One missing charger found in room 14.",user:"Team A",time:"10:18 AM"},{school:"Star Lake",area:"Area 4",text:"COW near library has 2 damaged devices. Replacement devices requested.",user:"Team B",time:"11:05 AM"}];
let selectedSchoolName=null;const areas=["All Areas",...Object.keys(areaMap)];const pct=s=>Math.round(s.done/s.total*100);
function status(s){const p=pct(s);if(p>=85)return["Almost Complete","good","var(--green)"];if(p>=45)return["In Progress","mid","var(--yellow)"];return["Needs Support","bad","var(--red)"]}
function fillSelect(select,options){select.innerHTML=options.map(o=>`<option value="${o}">${o}</option>`).join("")}
function filteredSchools(areaValue,searchValue=""){const q=searchValue.toLowerCase();return schools.filter(s=>(areaValue==="All Areas"||s.area===areaValue)&&s.name.toLowerCase().includes(q))}
function renderDashboard(){const area=globalAreaFilter.value,search=dashboardSearch.value,data=filteredSchools(area,search);const totalSchools=data.length,completed=data.filter(s=>pct(s)>=100).length,avg=totalSchools?Math.round(data.reduce((a,s)=>a+pct(s),0)/totalSchools):0,cowsDone=data.reduce((a,s)=>a+s.done,0),cowsTotal=data.reduce((a,s)=>a+s.total,0),damaged=data.reduce((a,s)=>a+s.damaged,0);metricSchools.textContent=totalSchools;metricCompleted.textContent=completed;metricProgress.textContent=avg+"%";metricCows.textContent=`${cowsDone} / ${cowsTotal}`;metricDamaged.textContent=damaged;schoolRows.innerHTML=data.map(s=>{const p=pct(s),[label,cls,color]=status(s);return`<div class="row"><div><div class="school-name">${s.name}</div><div class="small">${s.area} · ITS: ${areaLeads[s.area]}</div></div><strong>${p}%</strong><div class="progress"><div style="width:${p}%;background:${color}"></div></div><div>${s.done} / ${s.total}</div><div>${s.damaged}</div><span class="badge ${cls}">${label}</span></div>`}).join("");const almost=data.filter(s=>pct(s)>=85&&pct(s)<100).length,support=data.filter(s=>pct(s)<40).length,behind=[...data].sort((a,b)=>pct(a)-pct(b))[0];insights.innerHTML=`<div class="insight"><strong>${almost} school(s) almost complete</strong><br><span>Consider reallocating extra interns soon.</span></div><div class="insight"><strong>${support} school(s) need support</strong><br><span>${behind?behind.name:"No school"} is currently lowest progress.</span></div><div class="insight"><strong>Area selected</strong><br><span>${area==="All Areas"?"District-wide view":area+" · ITS: "+areaLeads[area]}</span></div>`}
function renderSchoolList(){const data=filteredSchools(detailAreaFilter.value,detailSearch.value);detailSchoolList.innerHTML=data.map(s=>{const active=s.name===selectedSchoolName?"active":"",[label,cls]=status(s);return`<div class="school-item ${active}" data-school="${s.name}"><div class="school-name">${s.name}</div><div class="small">${s.area} · ${pct(s)}% · ${s.done}/${s.total} COWs</div><span class="badge ${cls}">${label}</span></div>`}).join("");document.querySelectorAll(".school-item").forEach(item=>item.addEventListener("click",()=>{selectedSchoolName=item.dataset.school;renderSchoolList();renderDetailPanel()}))}
function renderAreaOnly(){const area=detailAreaFilter.value,data=filteredSchools(area,detailSearch.value);const avg=data.length?Math.round(data.reduce((a,s)=>a+pct(s),0)/data.length):0,cowsDone=data.reduce((a,s)=>a+s.done,0),cowsTotal=data.reduce((a,s)=>a+s.total,0),damaged=data.reduce((a,s)=>a+s.damaged,0);areaTitle.textContent=area==="All Areas"?"All Areas Snapshot":`${area} Snapshot`;areaMeta.textContent=area==="All Areas"?"District-wide area view":`ITS: ${areaLeads[area]}`;areaSchoolsCount.textContent=data.length;areaAvgProgress.textContent=avg+"%";areaCows.textContent=`${cowsDone} / ${cowsTotal}`;areaDamaged.textContent=damaged;areaSnapshot.innerHTML=data.map(x=>{const [label,cls,color]=status(x);return`<div class="snapshot-row"><span>${x.name}</span><strong>${pct(x)}%</strong><div class="progress"><div style="width:${pct(x)}%;background:${color}"></div></div></div>`}).join("")}
function renderSchoolOnly(){const s=schools.find(x=>x.name===selectedSchoolName);if(!s)return clearSelectedSchool();detailName.textContent=s.name;detailMeta.textContent=`${s.area} · ITS: ${areaLeads[s.area]} · ${s.total} COWs`;detailProgress.textContent=pct(s)+"%";detailCows.textContent=`${s.done} / ${s.total}`;detailDamaged.textContent=s.damaged;detailInterns.textContent=s.interns;detailBar.style.width=pct(s)+"%";const schoolNotes=notes.filter(n=>n.school===selectedSchoolName);detailNotes.innerHTML=schoolNotes.length?schoolNotes.map(n=>`<div class="note-card"><strong>${n.school}</strong><div>${n.text}</div><div class="note-meta">${n.user} · ${n.time}</div></div>`).join(""):`<div class="empty-note">No notes have been added for this school yet.</div>`;liveUpdates.innerHTML=`<div class="note-card"><strong>Current status</strong><div>${s.done} of ${s.total} COWs completed. ${s.total-s.done} COWs remaining.</div></div><div class="note-card"><strong>Damage update</strong><div>${s.damaged} damaged devices currently reported.</div></div>`}
function renderDetailPanel(){if(selectedSchoolName){areaOnlyView.classList.add("hidden");schoolOnlyView.classList.remove("hidden");renderSchoolOnly()}else{schoolOnlyView.classList.add("hidden");areaOnlyView.classList.remove("hidden");renderAreaOnly()}}
function clearSelectedSchool(){selectedSchoolName=null;renderSchoolList();renderDetailPanel()}
function renderUpdateSchoolOptions(){const opts=filteredSchools(updateArea.value).map(s=>s.name);updateSchool.innerHTML=opts.map(o=>`<option value="${o}">${o}</option>`).join("");updateMobileStats()}
function updateMobileStats(){const s=schools.find(x=>x.name===updateSchool.value)||filteredSchools(updateArea.value)[0];if(!s)return;updateSchool.value=s.name;updateCows.textContent=`${s.done} / ${s.total}`;updateDamaged.textContent=s.damaged}
function editSchool(field,amount){const s=schools.find(x=>x.name===updateSchool.value);if(!s)return;s[field]=Math.max(0,s[field]+amount);if(field==="done")s[field]=Math.min(s[field],s.total);updateMobileStats();renderDashboard();renderSchoolList();renderDetailPanel();renderSummary()}
function saveNote(){const text=noteBox.value.trim();const s=schools.find(x=>x.name===updateSchool.value);if(!s||!text){saveMessage.textContent="Add a note before saving.";setTimeout(()=>saveMessage.textContent="",1800);return}notes.unshift({school:s.name,area:s.area,text,user:"Jayden",time:new Date().toLocaleTimeString([],{hour:"numeric",minute:"2-digit"})});selectedSchoolName=s.name;noteBox.value="";saveMessage.textContent="Saved to school notes and end-of-day summary.";setTimeout(()=>saveMessage.textContent="",2200);renderSchoolList();renderDetailPanel();renderSummary()}
function noteMatchesSummary(note){const area=summaryAreaFilter.value,selected=summarySchoolFilter.value,search=summarySearch.value.toLowerCase();return(area==="All Areas"||note.area===area)&&(selected==="All Schools"||note.school===selected)&&note.school.toLowerCase().includes(search)}
function renderSummaryControls(){const opts=["All Schools",...filteredSchools(summaryAreaFilter.value,summarySearch.value).map(s=>s.name)];const current=summarySchoolFilter.value;summarySchoolFilter.innerHTML=opts.map(o=>`<option value="${o}">${o}</option>`).join("");if(opts.includes(current))summarySchoolFilter.value=current}
function renderSummary(){renderSummaryControls();const area=summaryAreaFilter.value,selected=summarySchoolFilter.value,search=summarySearch.value;let data=filteredSchools(area,search);if(selected&&selected!=="All Schools")data=data.filter(s=>s.name===selected);const visited=data.filter(s=>s.done>0).length,completed=data.filter(s=>pct(s)>=100).length,cowsDone=data.reduce((a,s)=>a+s.done,0),cowsTotal=data.reduce((a,s)=>a+s.total,0),damaged=data.reduce((a,s)=>a+s.damaged,0),avg=data.length?Math.round(data.reduce((a,s)=>a+pct(s),0)/data.length):0,lowest=[...data].sort((a,b)=>pct(a)-pct(b)).slice(0,3);summaryContent.innerHTML=`<strong>${selected==="All Schools"?"Summary":selected+" Summary"}</strong><br>Area view: ${area}<br>Schools shown: ${data.length}<br>Schools visited: ${visited}<br>COWs completed: ${cowsDone} / ${cowsTotal}<br>Average progress: ${avg}%<br>Damaged devices found: ${damaged}<br>Schools completed: ${completed}<br><br><strong>Top needs</strong><br>${lowest.map(s=>`${s.name}: ${pct(s)}% complete, ${s.total-s.done} COWs remaining`).join("<br>")||"No matching schools."}<br><br><strong>Recommended action</strong><br>Move interns away from schools above 85% completion and prioritize the lowest-progress sites.`;const visibleNotes=notes.filter(noteMatchesSummary);summaryNotes.innerHTML=visibleNotes.length?visibleNotes.map(n=>`<div class="note-card"><strong>${n.school}</strong><div>${n.text}</div><div class="note-meta">${n.area} · ${n.user} · ${n.time}</div></div>`).join(""):`<div class="empty-note">No notes match the current filters.</div>`}
function init(){fillSelect(globalAreaFilter,areas);fillSelect(detailAreaFilter,areas);fillSelect(updateArea,areas);fillSelect(summaryAreaFilter,areas);detailAreaFilter.value="Area 4";updateArea.value="Area 4";globalAreaFilter.addEventListener("change",renderDashboard);dashboardSearch.addEventListener("input",renderDashboard);detailAreaFilter.addEventListener("change",()=>{selectedSchoolName=null;renderSchoolList();renderDetailPanel()});detailSearch.addEventListener("input",()=>{selectedSchoolName=null;renderSchoolList();renderDetailPanel()});clearSchoolBtn.addEventListener("click",clearSelectedSchool);deselectFromDetail.addEventListener("click",clearSelectedSchool);updateArea.addEventListener("change",renderUpdateSchoolOptions);updateSchool.addEventListener("change",updateMobileStats);saveUpdate.addEventListener("click",saveNote);plusCow.addEventListener("click",()=>editSchool("done",1));minusCow.addEventListener("click",()=>editSchool("done",-1));plusDamage.addEventListener("click",()=>editSchool("damaged",1));minusDamage.addEventListener("click",()=>editSchool("damaged",-1));summaryAreaFilter.addEventListener("change",renderSummary);summarySchoolFilter.addEventListener("change",renderSummary);summarySearch.addEventListener("input",renderSummary);copySummary.addEventListener("click",()=>alert("Summary copied in the real version."));document.querySelectorAll("nav button").forEach(btn=>btn.addEventListener("click",()=>{document.querySelectorAll("nav button").forEach(b=>b.classList.remove("active"));document.querySelectorAll(".view").forEach(v=>v.classList.remove("active-view"));btn.classList.add("active");document.getElementById(btn.dataset.view).classList.add("active-view");pageTitle.textContent=btn.textContent}));renderDashboard();renderSchoolList();renderDetailPanel();renderUpdateSchoolOptions();renderSummary()}init();
